{
  "name": "yawn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "addons/*"],
  "scripts": {
    "start": "vitepress dev docs --host 0.0.0.0 --port ${PORT:-8080}"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
