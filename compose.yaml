services:
  app:
    build:
      context: .
      target: development
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - cargo_target:/app/target

volumes:
  node_modules:
  cargo_target:
