[package]
name = "yawn-core"
version = "0.1.0"
description = "Shared render-data arena and render-graph compiler"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
wasm-bindgen = "0.2"
